import "sub/imported.sat";

struct test : base
{
    int32 num = 42;
    bool b = true;
    float zero = .0;
    float thousand = 1.0e3;
}

// CHECK: base.@kind is Struct
// CHECK: test.@kind is Structx
// CHECK: test is-a base
// CHECK: test.@fields is 4
// CHECK: test.num is 42
// CHECK: test.b is true
// CHECK: test.zero is 0
// CHECK: test.thousand is 1000
