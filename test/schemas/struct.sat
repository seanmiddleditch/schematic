import "sub/imported.sat";

struct test : base
{
    int32 num = 42;
    bool b = true;
    float zero = .0;
    float thousand = 1.0e3;
}

// CHECK: base.@kind is Struct
// CHECK: test.@kind is Struct
// CHECK: test is-a base
// CHECK: test.@fields is 4
// CHECK: test.num.@default is 42
// CHECK: test.b.@default is true
// CHECK: test.zero.@default is 0
// CHECK: test.thousand.@default is 1000
